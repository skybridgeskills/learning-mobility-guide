# from https://github.com/BretFisher/jekyll-serve/blob/main/docker-compose.yml
# no version needed since 2020

services:
  jekyll:
    image: bretfisher/jekyll-serve
    volumes:
      - .:/site
    ports:
      - "4000:4000"
